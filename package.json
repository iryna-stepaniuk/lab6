{
  "name": "lab6",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "deploy": "npx firebase deploy --token $FIREBASE_TOKEN",
    "postinstall": "npm i --prefix ./functions"
  },
  "dependencies": {
    "firebase": "^7.5.2",
    "firebase-admin": "^8.8.0",
    "firebase-tools": "^7.9.0"
  }
}
